<% if is_active?("books", "index") %>
	<% if current_user.admin? %>
	|
	<%= link_to "delete", book,
				:method => :delete,
				:confirm => "Are you sure you want to delete this book?",
				:title => "Delete #{book.title} by #{book.author}" %>
	<% end %>
<% end %>
