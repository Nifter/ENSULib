<h1>All users</h1>

<%= will_paginate @users %>

<ul class="users">
	<%# Automatically takes the paginated users instance and iterates over it,
	sending each individual user to the _user partial %>
	<%= render @users %>
</ul>

<%= will_paginate @users %>
